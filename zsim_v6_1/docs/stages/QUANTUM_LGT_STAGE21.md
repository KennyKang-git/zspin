# QUANTUM_LGT_STAGE21

## Purpose

Stage-21 extends the Stage-20 live sweep expansion into a **broader default-live bridge**. The emphasis is not a new continuum claim, but a tighter orchestration layer that:

1. reuses the Stage-13 `default_broad` winner,
2. reuses the Stage-14 `larger_shape_sweep` winner,
3. reconnects both references to the Stage-20 live-sweep ledger.

The result is a direct ledger for **default-vs-broader live stability** without introducing a deeper physical claim.

## New components

- `zsim.lgt.stage21`
- `zsim.apps.su2_mbp_stage21_default_live_validate`
- `tests/test_lgt_stage21.py`
- `tests/test_lgt_stage21_app.py`

## Output artifacts

The stage app writes:

- `stage21_live_rows.csv`
- `stage21_control_rows.csv`
- `stage21_summary.json`

## Interpretation

This remains a **preproduction surrogate**. Stage-21 should be read as a bridge layer over the existing reduced-HPC surrogate stack, not as a final continuum overlap or Higgs-bilinear closure claim.
